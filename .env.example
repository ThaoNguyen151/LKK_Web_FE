# API Configuration
VITE_API_BASE_URL=http://localhost:3000/api

# Thư viện ảnh (optional). Nếu set, FE GET JSON [{ id, src, alt }] hoặc { items: [...] }.
# Không set → dùng ảnh trong src/assets/library (xem docs/LIBRARY_IMAGES.md).
# VITE_LIBRARY_API_URL=https://api.example.com/library/images

# App Configuration
VITE_APP_NAME=LKK Web
